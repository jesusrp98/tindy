name: tindy
description: Like Tinder, but swapping human beings, with powerful feline creatures.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cached_network_image: ^0.7.0
  dio: ^2.1.0

  scoped_model: ^1.0.1
  share: ^0.6.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true